{
  "name": "cryptosomething",
  "version": "1.0.0",
  "license": "Apache",
  "private": true,
  "main": "index.js",
  "dependencies": {
    "express": "^4.18.1"
  },
  "devDependencies": {},
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ilyaem/cryptoboom.git"
  },
  "engines": {
    "node": "16.14.0"
  }
}
